# دليل تعديل موقع الهنا اليكتريك

الموقع ملف واحد اسمه `index.html` ومجلد صور اسمه `img`. لا يحتاج برامج
ولا تنصيب — تعدّل الملف وترفعه، فيظهر التعديل خلال دقيقة.

داخل الملف كل موضع قابل للتعديل عليه علامة **✏️** ومكتوب فوقه ماذا يفعل.
ابحث في الملف عن `✏️` لترى كل المواضع.

---

## ١) تغيير الصور — الأسهل والأكثر أثرًا

**لا تفتح `index.html` إطلاقًا.** استبدل الملف في مجلد `img` بنفس الاسم.

| الملف | الصورة المطلوبة | تظهر أين |
|---|---|---|
| `panel-busbar.jpg` | أفضل صورة عندك | الواجهة والمعرض |
| `panel-cabinet.jpg` | لوحة توزيع فرعية | المنتجات والمعرض |
| `ph-mdp.svg` | لوحة توزيع رئيسية | المنتجات |
| `ph-ats.svg` | لوحة ATS | المنتجات |
| `ph-lighting.svg` | لوحة إنارة | المنتجات |
| `ph-control.svg` | لوحة تحكم | المنتجات |
| `ph-riser.svg` | صندوق رايزر | المنتجات |
| `ph-tray.svg` | حوامل كابلات | المنتجات |
| `ph-factory.svg` | داخل المصنع | المعرض |
| `ph-site.svg` | موقع تركيب | المعرض |
| `logo.png` | الشعار | الترويسة والتذييل |

الملفات التي تبدأ بـ `ph-` صور مؤقتة رمادية تنتظر صورك.

> **لو صورتك `.jpg` والملف المؤقت `.svg`:** ارفعها باسم `ph-mdp.jpg`،
> ثم افتح `index.html` وابحث عن `ph-mdp.svg` وغيّرها إلى `ph-mdp.jpg`
> في كل موضع تجدها فيه.

**قبل الرفع:** اضغط الصور. صورة ٥ ميجا تفتح الصفحة ببطء على الموبايل.
حجم مناسب: عرض ١٢٠٠ بكسل وأقل من ٣٠٠ كيلوبايت.

---

## ٢) تغيير أرقام التليفون

الأرقام مكتوبة في أكثر من موضع. استخدم **بحث واستبدال** في محرّر النصوص
بدل البحث اليدوي:

| ابحث عن | استبدله بـ |
|---|---|
| `201003769727` | رقم واتساب بكود الدولة وبلا `+` |
| `+201050559450` | رقم المبيعات الأول بكود الدولة |
| `+201050559451` | رقم المبيعات الثاني |
| `010 5055 9450` | شكل الرقم الأول كما يُعرض للزائر |
| `010 5055 9451` | شكل الرقم الثاني |
| `elhanaelectric@gmail.com` | البريد |

> كل رقم له شكلان: واحد للاتصال (`+2010...`) وواحد للعرض (`010 5055 9450`).
> غيّر الاثنين وإلا ظهر رقم واتصل برقم آخر.

---

## ٣) النصوص واللغتين

كل نص مكتوب مرتين في سطر واحد:

```html
<span data-ar>لوحات التحكم</span><span data-en-only>Control panels</span>
```

عدّل الاثنين معًا. لو عدّلت العربي فقط، يبقى الإنجليزي قديمًا ويراه
الزائر الأجنبي.

---

## ٤) الأرقام في الشريط الأزرق

ابحث عن `✏️ أرقام الشركة`. الأرقام الموجودة **تقديرية** — ضع أرقامك.

```html
<div class="num">+15</div>
<div class="lbl"><span data-ar>سنة خبرة في السوق</span>...
```

**لحذف خانة لا تعرف رقمها:** امسح كتلة `<div class="cell rise">` كاملة
حتى `</div>` المقابلة. خانة واحدة صادقة أقوى من أربع مخمّنة — المشتري
الصناعي يسأل عن الرقم في أول اجتماع.

---

## ٥) الماركات

ابحث عن `✏️ ماركات المكوّنات`.

```html
<span class="brand">ABB</span>
<span class="brand">Schneider Electric</span>
<span class="brand">CHINT</span>
```

انسخ سطرًا وغيّر الاسم لإضافة ماركة. **لا تُضف ماركة غير مستخدمة فعلًا.**

---

## ٦) القطاعات والمنتجات

كل قطاع كتلة `<div class="sector rise">`، وكل منتج كتلة `<article class="item">`.

- **للحذف:** امسح الكتلة كاملة من فتحها إلى إغلاقها
- **للإضافة:** انسخ كتلة موجودة والصقها بعدها وغيّر نصوصها

انتبه أن تنسخ الكتلة **كاملة** بكل وسوم الإغلاق، وإلا اختلّ شكل الصفحة.

---

## ٧) مواعيد العمل والعنوان

ابحث عن `✏️ بيانات التواصل`. المواعيد المكتوبة **تقديرية** — صحّحها.

---

## ٨) رقم استقبال طلبات عرض السعر

في أسفل الملف داخل السكربت:

```js
var SALES = "201003769727";
```

بكود الدولة وبلا `+`.

---

## الرفع

1. افتح `github.com/rafatabdo854-ai/Elhana-Electric`
2. **Add file** ← **Upload files**
3. اختر الملف المعدّل ← **Commit changes**
4. انتظر دقيقة ثم افتح `elhana.pages.dev`

لو لم يتغيّر شيء: امسح ذاكرة المتصفح للموقع، أو افتحه في نافذة خاصة.

---

## إن كسرت الصفحة

كل نسخة محفوظة في GitHub. افتح تبويب **Commits**، اختر آخر نسخة كانت
تعمل، ثم **Browse files** ← افتح `index.html` ← أيقونة `⋯` ← **Download**،
وارفعها من جديد.

لا يوجد تعديل لا يمكن التراجع عنه — هذه هي فائدة رفع كل تعديل.

---

## ما لا يُعدَّل إلا بحذر

- كل ما هو داخل `<style>` — يتحكم في شكل الصفحة كلها
- كل ما هو داخل `<script>` — يتحكم في القائمة والنموذج وعارض الصور
- الوسوم نفسها: `<div>` و`<span>` وأشباهها. عدّل **النص بينها** فقط
